enum Color {
    RED,
    GREEN = 5,
    BLUE
};

int main() {
    // For now, just test that enums parse correctly
    // Using the enum constants would require code generation support
    return 0;
}