typedef int my_int;
typedef char* string;
typedef int* int_ptr;

int main() {
    // For now, just test that typedefs parse correctly
    // Using the typedef'd types would require more complex implementation
    return 0;
}